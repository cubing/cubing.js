# /.cache is for Parcel 1, but the folder might stick around if you switch to an
# old checkout.
/.cache
/.parcel-cache
/.rpt2_cache
/coverage
/dist
/node_modules
/temp

# Legacy package folders.
# We can hopefully get rid of this once enough bundlers support the `exports` field in `package.json`.
/alg
/bluetooth
/kpuzzle
/notation
/protocol
/puzzle-geometry
/puzzles
/scramble
/search
/stream
/twisty
