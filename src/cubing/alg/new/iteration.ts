export enum IterationDirection {
  Forwards,
  Backwards,
}

// export function* iterateComposite(sources: {repeatable: Repeatable, direction: IterationDirection}[]): Generator<LeafUnit> {
//   for (const source of sources) {
//     for (const leaf of)
//   }
// }
