/*
If you have come across this file, it may be because your project is not
configured to resolve `"exports"` in `package.json` for dependencies. Consider
using the following settings in a `tsconfig.json` file for your project:

```
{
  "compilerOptions": {
    "lib": ["es2020", "dom"],
    "module": "es2020",
    "moduleResolution": "bundler"
  }
}
```

For more details, see: https://js.cubing.net/cubing/index.html#javascript
*/

/** @deprecated If you are seeing this definition, you are not using the correct types for `cubing.js`. Consider
using the following settings in a `tsconfig.json` file for your project:

```
{
  "compilerOptions": {
    "lib": ["es2020", "dom"],
    "module": "es2020",
    "moduleResolution": "bundler"
  }
}
```

For more details, see: https://js.cubing.net/cubing/index.html#javascript */
export type randomScrambleForEvent = {};
